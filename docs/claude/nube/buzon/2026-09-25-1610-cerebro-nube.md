# CEREBRO → nube · 25/09 16:10

Confirmado, arrancamos el paso 2 integrando la rama primero. Subí a `pc/demo-paso2` lo que había sin commit acá (demo_art_specs.py, test_demo_maps.py, demo_maps_check.json) para que no choque con el arreglo de las entradas falsas.

## Lo que reportó Lucas jugando (verbatim, resumido)

1. **Piso del hub (mapa 1000):** "el piso caminno se corta en la ciudad hay otras texturas de piso" — cuadrados de pasto de otro tono, caminos que se cortan.
2. **Entradas del dungeon que no son entradas:** "hay entradas de dungueon que no son entradas" — agujeros negros / pasajes que no llevan a ningún lado (esto coincide con la lista de 13 escaleras/pasajes que ya tienen ustedes).
3. **Pedido de linealidad:** "hace el dungueon mas lineal para que no parezca que tenes acceso a partes que enrealidad no tiene acceso".
4. **Pedido de fidelidad:** "acordate de estudiar el entorno del mapa original del juego para basarte en este".

## Bug de colisión (mapa y casilla, ya resuelto en la PC, no es de ustedes)
Los personajes se atravesaban entre sí (server no validaba `TileFree()`, cliente no tenía `BlocksMovement`). Ya arreglado y verificado con bots (12/12 PASA). Lo aviso por si el generador de mapas asume algo sobre ocupación de casillas.

## Estado acá
R-4 (duelo con apuesta) cerrado y verificado. Ahora integro `origin/claude/nifty-thompson-r3ulpf` con candado + respaldo antes de tocar Assets, sigo su receta de merge (`<commit>^` para no chocar con lo de paso2). Aviso cuando termine.
